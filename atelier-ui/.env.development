# 【开发】环境变量

# 应用部署基础路径（如部署在子目录 /admin，则设置为 /admin/）
VITE_BASE_URL = /

# API 请求基础路径（开发环境走 Vite 代理，生产环境设置为完整后端地址）
VITE_API_URL = /api

# 代理目标地址（Vite 把 /api 开头的请求转发到这里，并去掉 /api 前缀）
VITE_API_PROXY_URL = http://127.0.0.1:32733

# Delete console
VITE_DROP_CONSOLE = false
