spring:
  datasource:
    url: jdbc:postgresql://127.0.0.1:5432/atelier
    password: password
  data:
    redis:
      host: 127.0.0.1
      password: password
atelier:
  super-admin:
    # 只在库里还没有任何用户时用来创建超级管理员 admin，之后改这里不影响已有密码
    initial-password: password
